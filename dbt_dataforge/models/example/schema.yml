version: 2


sources:
  - name: bronze
    database: DATAFORGE_DB
    schema: BRONZE
    tables:
      - name: orders

models:
    - name: fct_orders
      description: "Final orders fact table"
      columns: 
        - name: order_id
          tests:
            - not_null
            - unique
        - name: delivery_status
          tests:
            - not_null
            